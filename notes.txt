BUGS:
    
    -Why are we passing tree into 'inventory_window()'?
    
    -Multiple windows can be open at a time because we are importing funcs and passing params
        -This will be fixed with classes
    -Could add error handling for spell book type stuff. If pure melee, continue, etc
    -if 2 chars are the same name, this creates an edit anomoly. We need to create some sort of unique match, or match on all rows to soften the blow
    -Remove console logs from inventory funcs

WHERE WE LEFT OFF:

TODO:
   
-ask kas for OOP advice

-Add lvl col
-Timestamps so i dont re parse
-open individual inventory is bugging out
-Maybe add individual char query
-Add query for individual char_list
-Filters for item searches
    -Advanced filtering

--BIG REFACTOR IDEAS:
    -inventory_window():
        -Add a checker at the beginning of window launch to see if selected char does not exist in DB, and abort on the spellbooks_object
            -This could be achieved by creating some metadata on app load.
            -For example, store the names of each char that exists in each table
            -This metadata can be accessed easily without performing additional queries and such

OOP OVERHAUL:
    -Classes:
        -MAIN_GUI:
        -INVENTORY_WINDOW_GUI
        -SPELLS_WINDOW_GUI
        -Methods:
            -CRUD:
                -Create Char
                -Edit Char
                -Delete Char
            -INVENTORY:
                -Parse Inventory(s)
                -Delete Inventory(s)
                -Open Inventory Window
            -FILE MENU:
                -Eq_dir
                -Campout Parser


GUI'S: (possible bundle this into a single class)
    class 'Gui':
        class 'Main_gui':
            -Call 'setup_main_gui' func on '__innit__'

        class 'Inventory_gui':
            -These widgets are contained within the 'Inventory Window' func
            -Refactor 'Inventory_Window' to have a 'setup_inventory_gui' func
                -Could call this func on '__innit__'
            

        class 'Spells_gui':
            -These widgets are contained within the 'Spells Window' func
            -Refactor 'Spells_Window' to have a 'setup_spells_gui' func
                --Could call this func on '__innit__'

CHARACTERS:
    class 'Character_methods':
        -Create
        -Edit
        -Delete
        -Parse Camp Locations

SPELLS:
    class 'Spells_methods':
        -Parse All Spells DB'S
        -Delete All Spells DB's

INVENTORY:
    class 'Inventory_methods':
        -Parse Inventory(s)
        -Delete Inventory(s)


--REFACTOR:
    -'main_gui_instance' to 'main_gui'
    -Any parameter 'eq_dir' can be refactored to 'self.data.eq_dir'







